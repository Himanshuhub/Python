'''
# Acquire raw materials (variables) needed for creating a car
a = 3
b = 5

# Send the raw materials(invoke and pass arguments) to a car manufacturing plant(function)
def add(a,b):
    # a = 3
    # b = 5
    # x = 0
# Do something (process) with the raw material (parameters)
    x = a + b
    # print x
# Drive the car (functions return value)
    return x
    # return = a + b

print add(a,b)

# result = add(3,5)
# print result

# Acquire raw materials (variables) needed for creating a car
# Send the raw materials(invoke and pass arguments) to a car manufacturing plant(function)
# Do something (process) with the raw material (parameters)
# Drive the car (functions return value)
'''
# def say_hi():
#     return "Hi"
#
# print say_hi()
# greeting = say_hi()
# print greeting

#     print "Hi " + name
#
# say_hi("Anna")

def add(a,b):
    x = a + b
    return x

sum1 = add(4,6)
# sum2 = add(1,4)
# sum3 = sum1 + sum2
